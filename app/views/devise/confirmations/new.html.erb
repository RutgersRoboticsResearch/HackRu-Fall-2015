<h1 class="form_head">Resend confirmation instructions</h1>
<fieldset class="small-form">
<%= simple_form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

    <%= f.input :email, input_html: { class: 'form-input', id: 'email', placeholder: 'Enter your email' } %>

  <div class="form-actions">
    <%= f.submit "Resend", class: 'registerBtn registerBtn-1' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
</fieldset>
