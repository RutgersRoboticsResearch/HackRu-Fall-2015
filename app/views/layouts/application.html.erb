<!DOCTYPE html>
<html>
<head>
  <title>HACKRU | Fall 2015</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= favicon_link_tag 'favicon.ico' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <!--NAV BEGIN -->
  <nav id = "nav">
    <div id = "nav_center">
      <%= link_to "about", homes_path(@home, :anchor => "about"), {:class=>"nav_text"}%>
      <%= link_to "sponsors", homes_path(@home, :anchor => "sponsors"), {:class => 'nav_text'}%>
      <%= link_to "faq", homes_path(@home, :anchor => "faq"), {:class => 'nav_text'}%>
      <%= link_to "schedule", homes_path(@home, :anchor => "schedule"), {:class => 'nav_text'}%>
      <%= link_to "team", homes_path(@home, :anchor => "team"), {:class => 'nav_text'}%>
      <%= link_to "map", "https://www.google.com/maps/place/Rutgers+College+Avenue+Student+Center/@40.5027331,-74.4520898,15z/data=!4m2!3m1!1s0x0:0x8820e7c83ea74250" , {:target => "_blank", :class => "nav_text"}%>
    </div>

    <ul id="signup">
      <% if user_signed_in? %>
      <%= link_to "My Profile", edit_user_registration_path, class: 'registerBtn2 registerBtn-1'%>
      <%= link_to "Log Out", logout_path, class: 'registerBtn2 registerBtn-1' %>   
      <% else %>
      <%= link_to "Sign up", register_path, class: 'registerBtn2 registerBtn-1' %>
      <%= link_to "Sign in", login_path, class: 'registerBtn2 registerBtn-1'%>
      <% end %>
    </ul>
  </nav>

  <div class="container">
    <%= render_breadcrumbs %>
    <%flash.each do |type, message|%>
    <div class="alert <%= flash_class type %>">
      <button class="close" data-dismiss="alert">x</button>
      <%= message %>
    </div>
    <% end %>
  </div>
  <%= yield %>
  <!--FOOTER BEGIN -->
  <section id="footer">
  </section>
  <!-- FOOTER END -->
</body>
<script id="activities-template" type="text/x-handlebars-template">
<a class="dropdown-toggle" href='#'>Activity Feed({{count}})</a>
<ul class="dropdown-menu">
{{#each activities}}
{{activityLink}}
{{/each}}
<li class="divider"></li>
<li><a href="{{activityFeedLink}}">Activity Feed</a></li>
</ul>
</script>

</html>

